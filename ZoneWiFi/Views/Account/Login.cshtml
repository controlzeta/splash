@model ZoneWiFi.Models.LoginModel

@{
    ViewBag.Title = "Iniciar sesión";
}
<div class="container">
    <div class="row" style="padding-top:20px;">
        <h1>@ViewBag.Title.</h1>
    </div>

    <div class="row" style="padding-top:20px;">
        <h2>Utilice una cuenta local para iniciar sesión.</h2>
        <div class="col-md-6" style="padding-top:20px;">
            @using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)


                <h2>Inicio de sesión</h2>
                <div class="row">
                    <div>
                        @Html.LabelFor(m => m.UserName)
                        @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", placeholder = "Usuario" })
                        @Html.ValidationMessageFor(m => m.UserName)
                    </div>
                    <div>
                        @Html.LabelFor(m => m.Password)
                        @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Contraseña" })
                        @Html.ValidationMessageFor(m => m.Password)
                    </div>
                    <div>
                        @Html.CheckBoxFor(m => m.RememberMe)
                        @Html.LabelFor(m => m.RememberMe, new { @class = "checkbox" })
                    </div>
                </div>
                <div class="row">
                    <input type="submit" value="Iniciar sesión" />
                </div>
                <div class="row">
                    <p class="text-white">
                        @Html.ActionLink("Regístrese", "Register", new { @class = "btn btn-primary text-white" }) si no tiene ninguna cuenta.
                    </p>
                </div>

            }
        </div>

        <div class="col-md-6" id="" style="padding-top:20px;">
            <h2>Usar otro servicio para iniciar sesión.</h2>
            @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
